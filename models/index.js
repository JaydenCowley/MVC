const User = require('./Users'); 
// const thought = require('./thought'); 

// User.hasMany(thought, {
//     foreignKey: 'userid',
// }); 

// thought.belongsTo(User, {
//     foreignKey: 'userid', 
// });

module.exports = { User };